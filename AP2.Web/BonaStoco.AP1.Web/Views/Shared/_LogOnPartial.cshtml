@if(Request.IsAuthenticated) {
    <text>Selamat Datang, <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Log Off", "LogOff", "Account") ]
    [ @Html.ActionLink("Change Password", "UbahPassword", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Log On", "LogOn", "Account") ]
}
