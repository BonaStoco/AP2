@model BonaStoco.AP1.Web.Models.ImportProductResponse
@{
    ViewBag.Title = "Edit Tenant";
}
<div>
@if (!Model.HasError)
{
    <div>
        <h2>Edit tenant berhasil. @Html.ActionLink("Kembali ke edit tenant.", "ImportEditTenant")</h2>
    </div>
}
else
{
    <div>
        @Html.ActionLink("Kembali ke edit tenant.", "ImportEditTenant")
        <br />
        <table>
            <caption>Edit tenant yang gagal:</caption>
            <thead>
                <tr>
                    <th>Pesan error</th>
                </tr>
            </thead>
            <tbody>
                @foreach (string errMsg in Model.ErrorMessages)
                {
                    <tr>
                        <td>@errMsg</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}
</div>